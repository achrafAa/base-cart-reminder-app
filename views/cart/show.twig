
{% block content %}
<h1>Cart</h1>
{% for item in cart.items %}
    <p>{{ item.product.title }} - {{ item.quantity }} - {{ item.price }}</p>
{% endfor %}

    <form action="/checkout/{{ cart.id }}" method="POST">
        <input type="submit" value="Checkout">
    </form>
{% endblock %}